import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yuvaldolev.chatgpt',
  appName: 'ChatGPT',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
